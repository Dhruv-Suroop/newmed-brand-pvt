**Logo / Symbol** — the real NewMed Skills mark. Coral symbol + bold "NewMed" / regular "Skills" wordmark. Never alter the lockup or collapse the name to one word.

```jsx
<Logo variant="horizontal" size={28} />
<Logo variant="symbol" size={40} />
<Symbol size={48} />
```

On dark surfaces set `color="var(--nm-cream)"`. Full-fidelity exported files live in `assets/logos/`.
