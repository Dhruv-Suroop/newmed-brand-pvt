**Select** — dropdown matched to Input's styling.

```jsx
<Select label="Profession" options={["Registered nurse","Allied health","Physician"]} />
```

Options are strings or `{value,label}`. Custom coral chevron; same focus ring as Input.
