**Input** — labelled text field with optional hint and error. Soft orange focus ring; error turns border and message orange.

```jsx
<Input label="Work email" placeholder="you@hospital.org" hint="We reply within one business day." />
<Input label="License number" error="This field is required." />
```

Pass `iconLeft` for a leading glyph. Labels are sentence case.
